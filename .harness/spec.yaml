schemaVersion: 2

name: collateral-rail
description: Preserve and extend the battle-tested ATS financing scaffold.
prd: .harness/prd.md
maxAttempts: 3

baseline:
  commands:
    - name: install
      command: corepack yarn install --immutable
      timeoutMs: 600000
    - name: contracts
      command: yarn foundry:test
      timeoutMs: 600000
    - name: web-build
      command: yarn next:build
      timeoutMs: 600000

validators:
  static: .harness/validators/static.json
  commands: .harness/validators/yarn.json
  playwright: .harness/validators/playwright-smoke.yaml

contract: .harness/acceptance-contract.json
validator:
  enabled: true

constraints:
  packageManager: yarn@3.2.3
  workspaces:
    - packages/foundry
    - packages/nextjs
  forbiddenWorkspaces:
    - packages/hardhat

forbiddenFiles:
  - .env
  - packages/foundry/.env
  - packages/nextjs/.env.local

secretScan:
  failOnFiles:
    - .env
    - packages/foundry/.env
    - packages/nextjs/.env.local
  patterns:
    - name: private-key-assignment
      pattern: "(PRIVATE_KEY|OPERATOR_KEY|MNEMONIC)\\s*=\\s*(0x)?[0-9a-fA-F]{32,}"
    - name: pem-private-key
      pattern: "BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY"

requiredFiles:
  - packages/foundry/contracts/AtsCollateralRail.sol
  - packages/foundry/contracts/oracle/PythHbarUsdOracle.sol
  - packages/foundry/contracts/verifiers/RailAcceptance.sol
  - packages/nextjs/app/facility/page.tsx
  - packages/nextjs/app/verify/page.tsx
  - docs/hedera-integration-field-guide.md
  - template.json
